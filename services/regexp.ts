export const regexpEmail =
	/^[a-zA-Z0-9._%+-]+@(?:[a-zA-Z0-9-]+\.)+[A-Za-z]{2,}$/;
export const regexpWords = /^[а-яА-Яa-zA-Z]+$/;
export const regexpNumber = /^\+?[0-9 ()-]+$/;
export const regexpFilterNumber = /[\s ()+-]+/g;
export const regexpPath = /\w/gs;
